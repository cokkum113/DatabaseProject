package com.example.makingboard.application.board.change;

import com.example.makingboard.application.board.dto.PosterRequest;
import com.example.makingboard.application.board.dto.PosterVO;
import com.example.makingboard.application.board.persistence.PosterRepository;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Getter;
import lombok.NoArgsConstructor;

@Getter
@AllArgsConstructor
@NoArgsConstructor
@Builder
public class EntityAsPoster {

    /*
    private PosterRepository posterRepository;

    public PosterVO changeRequestToPosterVO(PosterRequest posterRequest) {

    }

    저장할 때는 Entity로 바로 저장하는게 디비에 바로 저장되니 이렇게 바로 request요청이
    entity의 값으로 가게 해도되는지?

     */

}
